@{
    ViewData["Title"] = L["Tire Warehouse"].Value;
}

<div class="page">
    <div class="container">
        <div class="page-head">
            <div>
                <div class="eyebrow">Sklad</div>
                <h1 class="page-title">@L["Tire Warehouse"]</h1>
                <p class="page-lede">@L["Track stock, movements, and low-stock alerts across every SKU."]</p>
            </div>
            <div class="head-actions">
                <a asp-controller="Tires" asp-action="Index" class="btn btn-primary">@L["Open Inventory"]</a>
            </div>
        </div>
    </div>
</div>
